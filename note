Install Mono:

Since the code uses .NET libraries (CLR), you'll need to use Mono, an open-source implementation of the .NET Framework for Linux, to run .NET-based DLLs on Linux. Install Mono on your Linux system if it's not already installed:


# https://www.youtube.com/watch?v=lnsTJRY1jPU&ab_channel=SMKTECH
# https://www.thecoderworld.com/install-ubuntu-on-virtualbox/

sudo apt-get install mono-complete
sudo apt-get install mono-vbnc

<!-- 
The code you provided is written in C#. C# (pronounced "C sharp") is a programming language developed by Microsoft. It is a versatile language commonly used for developing Windows applications, web applications, and various types of software on the .NET framework. The code defines a namespace CalcProject containing a class named calculate with two methods, Add and Sub, which perform addition and subtraction operations on integers, respectively. -->



https://www.wikihow.com/Open-DLL-Files